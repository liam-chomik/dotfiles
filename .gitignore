# Editor and OS artifacts. Overlaps gitignore_global on purpose: that file only
# exists on machines where these dotfiles are installed, while this one travels
# with a clone.
*.swp
*.swo
*~
.DS_Store
